# adversaries: implementation with an adversarial reviewer.
# Chain: coder -> reviewer -> coder. Task mode only.
# Format: window <role> <agent-kind> <worktree> [task|batch] [extra-cli-args...]
window coder claude master
window reviewer claude reviewer
