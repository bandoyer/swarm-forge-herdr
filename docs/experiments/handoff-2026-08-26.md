# Session handoff — 2026-08-26

Read this first in a new Grok session. Repo: `bandoyer/swarm-forge-herdr`
(local `~/Work/swarm-forge-herdr`). Operator: bandoyer. Runtime: Herdr 0.8.2.

## Where things stand

Swarms are **down** (`swarm down`, not `retire`). Worktrees remain.

| Checkout | What it was | State |
|---|---|---|
| `~/Work/swarm-forge-herdr` | S5 six-pack on `six-codex-grok-fable-review` | Down. S5 **merged to `main`** (`18b67a0`). Independent smoke 324 on the QA tree before merge. |
| `~/Work/swarm-dogfood-gc` | Undo flagship: Grok spec, Sol coder | Down. Terminal candidate on role branches, **not** integrated to dogfood `main`. |
| `~/Work/swarm-dogfood-cg` | Undo invert: Sol spec, Grok coder | Down. Same. |
| `~/Work/swarm-dogfood` | Original dogfood, adversaries-codex | Untouched; do not `up` it for these experiments. |

`main` is **ahead of `origin/main`** (unpushed). Do not force-push. Push only if the operator asks.

S5 is **not retired**. `swarm retire` only after the operator is sure they do not need the `cgf-*` worktrees. Next pack switch will warn about stale worktrees (`cgf-*`, leftover `reviewer`).

## What we learned

### Undo experiment (dogfood, serial)

Two isolated six-packs, same slice (`docs/experiments/undo-slice.txt`): single-step `Undo` on `StackCalculator`.

| | Flagship `six-grok-codex-review` | Invert `six-codex-grok-review` |
|---|---|---|
| Specifier | Grok xhigh | Sol xhigh |
| Coder | Sol high | Grok high |
| Cleaner | Grok high | Sol high |
| Architect | Grok xhigh | Sol xhigh |
| Hardener | Grok high | Grok high |
| QA | Sol xhigh | Sol xhigh |
| Result | QA pass, 161 tests (independent green) | QA pass, 156 tests (independent green) |
| Wall clock | ~26 min | ~25 min once running |

**Invert won.** Sol wrote the tighter spec; Grok coded Evaluate isolation on the first implementation; Grok hardener rewrote null checks so Stryker could mutate Undo/MemoryRecall (100%, 0 survivors). Flagship Grok specifier left Undo-after-Swap unspecified; Sol specifier (after a tighter brief) pinned discard-on-unlisted-mutators.

Codex specifier **stalls at chain start**: `ready_for_next.sh` → `NO_TASK` and it refuses to work. Grok specifier takes the operator prompt. Always include in the first specifier prompt: *This is chain start. NO_TASK is expected. The operator message is the assignment.*

Codex specifier also **asks** when the brief is ambiguous (Swap/Dup/Evaluate vs undo). That is correct specifier behavior; it looks “stuck” if nobody answers. Run **serially**, not two six-packs in parallel (rate limits + trust dialogs + ask-the-user pauses).

Recommended Claude-free six-pack remains `six-codex-grok-review` (Sol spec/QA, Grok coder/hardener). Optional leftover Fable: **architect only**, not specifier or coder. Pack: `six-codex-grok-fable-review`.

### Quality bars

Everyone “running tests” was mostly everyone re-running **coverage/CRAP/jscpd/format**. Fast unit suite is cheap. Stock policy is now: universal bar = fast suite only; hardener owns metrics; QA does not re-collect lcov. Design: `docs/quality-bars.md`. Dogfood’s `project.prompt` still lists coverage as universal — project-local, not stock.

This repo (Babashka) has **no Stryker**. Hardener here runs **smoke**. Mutation is a hardener job only on toolset projects (e.g. dogfood `dotnet stryker`).

### S5 on this repo (`six-codex-grok-fable-review`)

Dead-worker reconciliation: `squadd` fail-closes malformed `herdr agent list`, K=3 misses, `:allocated` 15s grace, advisor **row 14 `report-orphan`**. Spec: `docs/squad-hardening-s5-spec.md`. Design: `docs/squad-hardening.md`.

QA **finding loop twice**, then pass 3:

1. Malformed agent-list shapes not fail-closed (incomplete spec on “usable list”).
2. Spec added row 14 but **forbade** `AGENTS.md`/`CLAUDE.md`; those files still said 10+3 exhaustive. Spec/scope bug from the operator brief’s path list.
3. Guides synced; QA passed. Independent smoke 324.

**Contract holes (operator, not model):** coder could not hand off because `swarmforge/roles/` then `AGENTS.md`/`CLAUDE.md` were outside artifact roots while smoke requires dual-write. Fixed on main (`0bc892d`, `d978190`). Keep those roots when switching packs.

Fable 5 high **did** architect: structure “sound,” small smoke-export fix (`f18bc2c`). Cheap review seat.

S5 `swarm up` did **not** block Codex on first-run trust the second time (folders already trusted).

## Next swarm — how to run it

Preset: **`six-mix-fable-review`** (already in `packs/`, smoke-pinned).

| Role | Provider | Model | Effort |
|---|---|---|---|
| specifier | Grok | grok-4.6 | **xhigh** |
| coder | Codex | gpt-5.6-sol | **high** |
| cleaner | Grok | grok-4.6 | high |
| architect | Claude | claude-fable-5 | high |
| hardener | Grok | grok-4.6 | high |
| qa | Codex | gpt-5.6-sol | xhigh |

Grok writes the law and cleans/hardens; Sol implements and attests; Fable only reviews structure.

### Procedure (serial, this repo)

1. Confirm `main` includes S5 (`18b67a0` or later) and the mix pack (`28f38b9`).
2. `swarm switch six-mix-fable-review` from `~/Work/swarm-forge-herdr`.
3. Re-apply **project-local** contracts if `switch` does not overwrite existing ones (`install!` is write-once). Coder/cleaner/architect/hardener roots must include `swarmforge/roles/`, `swarmforge/contracts/`, `AGENTS.md`, `CLAUDE.md`. Evidence for this repo is `SMOKE PASSED (N checks)`.
4. Commit `swarmforge/` **before** `swarm up` so linked worktrees see HEAD.
5. `swarm up`. If Codex panes idle with no bootstrap, `swarm bootstrap <role>`.
6. Wait until specifier is idle/done, then `swarm prompt specifier` with a **single slice** plus chain-start language (Grok specifier usually does not need it; keep it anyway).
7. Do **not** run a second six-pack in parallel.
8. Operator watches; do not poll every 30s with the LLM. A local watcher that only prints DONE/FAILED is free. Swarm agent tokens are the cost.
9. `swarm down` is a pause. Integrate the QA terminal commit to `main` yourself, then `swarm retire` only if every role branch is merged and queues are drained.

### Suggested first mix-pack slice

**S6** from `docs/squad-hardening.md` (worker agent kind is data) **or** **S7a** (daemon-only `merge` env gate). Both are bounded. S6 if the operator wants Grok/Codex squad workers later; S7a if they want mechanism actually daemon-only. Default: **S6** unless they pick S7a.

Do not implement S6–S8 in one slice. Specifier writes a spec with acceptance criteria and path list that **includes** `AGENTS.md`/`CLAUDE.md` if invariants live there.

### Specifier prompt shape

Include: task name, S6-or-S7a only, path allow-list that matches contracts, “NO_TASK is chain-start,” “do not implement other S-slices,” smoke must stay green, edit `prompts/` as source of truth and sync `swarmforge/roles/` in the same commit.

## Key docs

| Path | What |
|---|---|
| `docs/squad-hardening.md` | S5–S8 design |
| `docs/squad-hardening-s5-spec.md` | S5 acceptance spec |
| `docs/six-pack-grok-codex.md` | Undo experiment + recommended mixes |
| `docs/quality-bars.md` | Who runs which tests |
| `docs/experiments/undo-slice.txt` | Undo operator brief |
| `packs/six-mix-fable-review.conf` | Next pack |
| `AGENTS.md` | Invariants; keep in sync with `CLAUDE.md` |

## Do not

- Parallel two six-packs.
- Put Fable on specifier or coder.
- `swarm up` original `swarm-dogfood` for these tests.
- Bypass `swarm_handoff` when a contract rejects a dual-write; fix the roots.
- Run Stryker on this Babashka repo.
- Claim S5 is on `origin` until the operator pushes.
